function DragAndDrop {
	this._$dropArea = document.createElement ("div");
	this._$dropArea.classList = "dnd_drop_area"
	
	this.initialize = () => {
		let containers = document.querySelectorAll (".dnd_container");
		
		for (let i in containers) {
			containers[i].
		}
	};
};
